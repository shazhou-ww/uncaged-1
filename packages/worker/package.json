{
  "name": "@uncaged/worker",
  "version": "0.1.0",
  "description": "Unified Uncaged Worker — one codebase, N agent instances",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@uncaged/core": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260403.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2",
    "wrangler": "^4.80.0"
  }
}
